# Что осталось добрать по документам

Список того, чего не хватает в собранном инвентаре
(`stage-E-1/inventory/events.jsonl`, **270 событий** семейства E-1 за
2021–2025 из AZIPI) и что для этого нужно.

Собрано было 812; ревизия по текстам исключила 519 как не относящиеся к
семейству (обычный buyback по ст. 72 и отчёты постфактум) — они лежат в
`inventory/out_of_family.jsonl`, см. `coverage_report.md`, раздел
«Ревизия 2026-08-24». Ещё 23 исключены 2026-08-27 как документы-заглушки без
достоверной подписи (`inventory/unusable_documents.jsonl`) — см.
«Обновление 2026-08-27» там же про баг привязки эмитента к сообщению,
из-за которого эти числа и появились в текущем виде.

Машинный срез в любой момент:

```sh
uv run python scripts/validate_e1_inventory.py   # тип x год x источник x pending
```

## 1. Весь 2021 год и раньше — источник нужен другой

Исправлено 2026-08-27: прежнее «архив начинается 30.07.2021» само было
следствием того же бага (дата бралась из испорченной подписи страницы
поиска). После исправления самая ранняя дата в инвентаре — **13.01.2022**.
2021 год не «частично покрыт», а **отсутствует полностью** — все 23
события, изначально помеченные 2021 годом, оказались документами-
заглушками (см. `coverage_report.md`, «страницы сообщений AZIPI
нестабильны во времени»). За 2019–2020 поиск честно отдаёт ноль, это не
баг. Итого отсутствуют **все три года — 2019, 2020, 2021** из 7,
требуемых ТЗ, а не 2,6, как считалось раньше.

Живой поиск по 2021 году сообщения по-прежнему находит (26 штук по
четырём кодам), но получить их надёжно можно только забирая документ сразу
после того, как ссылка найдена, в одну сессию — иначе есть риск получить
такую же заглушку. Варианты: досбор 2021 года на AZIPI по этому правилу
(не сделан в этой сессии сознательно — риск наслоить непроверенное на
только что исправленное), архив Интерфакса (`e-disclosure.ru`, закрыт
антиботом для этой среды — см. `docs/OWNER_ACTIONS.md`),
`disclosure.ru`/`disclosure.1prime.ru` (доступны, поиск по типам не
работает — см. `coverage_report.md`), либо выгрузка с машины владельца.

## 2. Условия предложений — по 21 событию их нет в самом сообщении

Из 196 предложений 175 идут полным нумерованным шаблоном (цена, гарант,
приобретатель читаются из текста), а 21 — короткое уведомление вида
«поступило обязательное предложение», где условия лежат в **приложенном
файле**. Для них цена/гарант/приобретатель принципиально недостижимы из
скачанного сообщения — нужен сам файл предложения (ссылка обычно ведёт на
`disclosure.ru/issuer/<ИНН>/` или на сайт эмитента).

Получить список этих 21:

```sh
uv run python - <<'PY'
import json, pathlib
rows = [json.loads(l) for l in open('stage-E-1/inventory/events.jsonl', encoding='utf-8')]
for r in rows:
    if r['event_type'] == 'voluntary_or_mandatory_offer' and 'procedure_price' in r['pending_fields']:
        print(r['announcement_date'], r['issuer'][:60], r['source_document_refs'][0])
PY
```

### Путь к приложениям открыт, но узкий (измерено 2026-08-25)

Вложения читаются: `bsdtar` распаковывает RAR5 архивов disclosure.ru, PDF
рендерятся текстовым слоем или OCR (`scripts/setup_document_toolchain.sh`,
`scripts/fetch_disclosure_attachments.py`). Ограничение теперь не в формате,
а в охвате агентства.

Пробой всех **186 эмитентов** инвентаря по `disclosure.ru/issuer/<ИНН>/`
(`scripts/probe_disclosure_attachments.py`, результаты
`inventory/disclosure_ru_issuers.csv` и `disclosure_ru_documents.csv`):

| | эмитентов |
|---|---|
| страница есть, документов нет | 166 |
| документы есть, но не по процедурам E-1 | 15 |
| **документы процедур E-1 есть** | **5** |

Итого **14 документов у 5 эмитентов — 2,7% инвентаря**. Остальные 97,3%
раскрываются через Интерфакс. Путь рабочий, но он не закрывает выборку.

## 3. Ценовая база — почти всегда в приложении, не в сообщении

Из 121 найденного пункта о цене саму базу (биржевая средневзвешенная /
отчёт оценщика) называет **один**. Остальные дают только число. База — в
резолютивной части отчёта оценщика, который прикладывается отдельно.
Признак П-3 чек-листа («ценовая база чистая») без него не проверяется.

## 4. Разделение ст. 84.7 и 84.8 — только по тексту

Обе процедуры называются «требование о выкупе» и в таксономии агентства не
различаются, поэтому все 97 таких событий лежат в
`squeeze_out_request_95`, а `forced_buyback` пуст. Разделение — задача
экстракции по тексту, не индексации.

## 5. `buyback_art_75_76` — событий нет, нужен другой заход

Ревизия закрыла вопрос отрицательно: коды 4005185 / 1080045 к этому
семейству не относятся (подробности в `coverage_report.md`). Прямого типа
сообщения под ст. 75–76 в таксономии агентства нет — право требовать
выкупа возникает из решения общего собрания (реорганизация, крупная
сделка, делистинг), которое раскрывается в общей категории «Решения общих
собраний акционеров».

**Проверено исчерпывающе 2026-08-27:** полная таксономия AZIPI получена
напрямую (`ajax_get_facts_type_selector.php`, 312 типов сообщений без
фильтрации по группе — параметр `GROUP` эндпоинтом игнорируется, отдаёт
всё сразу). Кода для решения эмитента о реорганизации или о крупной сделке
**самого эмитента** в таксономии нет — есть только зеркальные типы для
контролирующих/подконтрольных организаций (1079243, 1079244, 1079260,
1079988, 1079989), которые к самому эмитенту не относятся. Ранее
проверенные кандидаты (1080045, 4005185) остаются отклонены. Это закрывает
вопрос: прямого машинно-фильтруемого пути к триггеру ст. 75 у AZIPI нет и
не появится.

Заодно найдены три ранее не индексированных кода того же семейства
voluntary_or_mandatory_offer: **1746** (направление предложения в Банк
России), **1747** (раскрытие содержания предложения в сети Интернет — тот
же пронумерованный шаблон, что и у уже собранных типов, с полем цены) и
**1079965** (конкурирующее предложение, ст. 84.5 — 0 событий за 2019–2025).
1746/1747 не добавлены в `collect_azipi.py` в этой сессии — сначала нужно
решить, дублируют ли они уже собранные 1079963/1079964 для того же
события (что сделает их источником подтверждения, не расширения выборки)
или называют события, которых нет под другими кодами.

**Путь пройден частично (2026-08-24):** делистинги взяты не у агентства, а
напрямую из истории листинга MOEX — `scripts/collect_delistings.py`,
результат `inventory/delisting_triggers.csv`: **73 делистинга акций,
64 эмитента, 2019–2025** (в том числе 11 за 2019 и 6 за 2020, куда AZIPI не
достаёт). Все по построению торгуемые.

Осталось: по каждому делистингу найти раскрытие эмитента (решение собрания
+ уведомление о праве требовать выкупа) с ценой и условиями. Для клиентов
АК&М — `disclosure.ru/issuer/<ИНН>/`; для остальных — Интерфакс.
Реорганизации (`1079194`) как второй триггер не обрабатывались.

## Чего делать НЕ нужно

Не пытаться закрыть pending-поля регулярками до конца. По ТЗ 6.3–6.5
структурная экстракция — предмет Stage E1: валидация схемы, закрепление
`extraction_model`/`prompt_version`, золотой регрессионный набор с ручной
проверкой эталонов. Ad-hoc парсинг в Фазе A положил бы незафиксированную,
непроверяемую экстракцию в основание всего исследования.

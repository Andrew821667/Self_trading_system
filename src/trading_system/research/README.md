# research

Scope (TZ sections 3 "Stage E3", 7): hypothesis registry, campaign budgets,
R3-A/R3-B protocols, walk-forward, Holdout A, sealed Vault B, R0-R6 gates.

Status: **not started**. Stage E3 opens after Stage E2 (Backtest & Execution
Simulation) is accepted, which itself opens after Stage E0/E1. All of those
are gated on a **CONFIRMED** verdict for the current EdgeThesis (TZ 31, step
6) — see `docs/tz/TZ_v2.0_FINAL.md` and `ROADMAP.md` at the repo root for the
current stage.

The E-1 event inventory + blind classification work (TZ 31, steps 2 and 4)
that precedes this package's existence lives outside the codebase, in
`docs/artifacts/` and the (not-yet-created) frozen dataset files it
describes.

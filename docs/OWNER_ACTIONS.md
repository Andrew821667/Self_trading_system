# Инструкции владельцу: два действия, разблокирующие вердикт E-1

Оба действия независимы. Первое дешёвое и может закрыть вопрос само по себе;
второе — запасной путь, если первого не хватит.

---

# Действие 1. Открыть домены в окружении (5 минут)

## Зачем

Прокси окружения режет на `403` до установления соединения три группы
хостов. Главный — **НРД**: он раскрывает обязательные предложения, выкупы и
вытеснения **по всей листингованной вселенной** и машиночитаемо. Это ровно та
популяция, которой нет в собранном инвентаре (91% его эмитентов не торгуются
на бирже).

| Хост | Что даёт |
|---|---|
| `nsddata.ru`, `nsd.ru` | корпоративные действия НРД по всем торгуемым бумагам |
| `isin.ru` | справочник ценных бумаг НРД |
| `bo.nalog.ru`, `egrul.nalog.ru` | признак П-5 чек-листа (история приобретателя) без платных агрегаторов |

## Как

1. Открыть **claude.ai/code**.
2. Нажать иконку облака — это селектор окружений. Найти окружение, в котором
   идёт эта сессия, и открыть его на редактирование.
3. Поле **Network access** уже стоит в **Custom** (иначе не работали бы
   `disclosure.ru`, `cbr.ru`, `moex.com`).
4. В поле **Allowed domains** **дописать** строки ниже к тому, что там уже
   есть. **Не заменять список** — иначе отвалятся текущие источники.

```
nsddata.ru
*.nsddata.ru
nsd.ru
*.nsd.ru
isin.ru
*.isin.ru
bo.nalog.ru
egrul.nalog.ru
```

5. Галочка **«Also include default list of common package managers»** должна
   остаться как есть.
6. Сохранить.

## Важные оговорки

- Одна строка — один домен. Ведущая `*.` покрывает все поддомены.
- Изменения подхватывает **новая сессия**; уже запущенная работает со старыми
  правилами. После сохранения напиши мне — если прокси всё ещё отдаёт `403`,
  значит сессию надо перезапустить.
- Если захочешь, чтобы я делал страницы-панели (artifacts) со статусом, в этот
  же список нужна строка `*.frame.claudeusercontent.com` — без неё я не смогу
  читать собственные опубликованные страницы.

## Что я сделаю после

Проверю доступ, найду у НРД ленту корпоративных действий по нужным типам,
соберу события 2019–2025 по торгуемым бумагам и обновлю отчёт покрытия. Если
НРД закроет пробел — Действие 2 не понадобится.

---

# Действие 2. Собрать архив Интерфакса на своей машине

Нужно, только если НРД не даст нужного. `e-disclosure.ru` обслуживает ~90%
листингованных эмитентов, но моей песочнице отдаёт JS-заглушку антибота вместо
данных. Обходить её я не стал: она поставлена владельцем сайта, и обходить
её — не то, что делают гости. Обычный браузер проходит проверку штатно, потому
что она на человека с браузером и рассчитана.

## Что именно нужно

Сообщения четырёх типов за 2019–2025. Коды — единая нумерация Банка России,
подтверждены в живой форме поиска:

| Код | Название в форме |
|---|---|
| 181 | Поступление эмитенту добровольного предложения о приобретении его эмиссионных ценных бумаг |
| 182 | Поступление эмитенту обязательного предложения о приобретении его эмиссионных ценных бумаг |
| 184 | Поступление эмитенту уведомления о праве требовать выкупа эмиссионных ценных бумаг эмитента |
| 185 | Поступление эмитенту требования о выкупе эмиссионных ценных бумаг эмитента |

## Вариант A. Вручную, без программирования — начни с него

Даже один год даст мне проверить формат раньше, чем ты потратишь вечер.

1. Открыть <https://www.e-disclosure.ru/poisk-po-soobshheniyam>.
2. В типах сообщений отметить четыре пункта из таблицы выше.
3. Диапазон дат — **по одному году за раз** (01.01.2019–31.12.2019 и так
   далее): у выдачи бывает потолок по числу результатов.
4. Каждую страницу выдачи сохранить: `Ctrl+S` → **«Веб-страница, только
   HTML»** → в папку `stage-E-1/documents/interfax/pages/`.
5. Как накопится — передать мне (см. «Как передать» ниже).

## Вариант B. Скриптом — когда формат подтверждён

В репозитории лежит `scripts/owner/collect_interfax.py`. Он поднимает
**настоящий Chromium** через Playwright, тот выполняет JS страницы как
задумано, заполняет форму и постранично сохраняет выдачу.

Скрипт написан вслепую: страницу поиска я ни разу не смог загрузить, поэтому
селекторы формы — обоснованная догадка. Поэтому у него два режима, и первый
обязателен.

```sh
# один раз
pip install playwright
playwright install chromium

# шаг 1 — разведка: открывает страницу, сохраняет её HTML и скриншот, и всё
python scripts/owner/collect_interfax.py --explore

# пришли мне search_page.html и search_page.png — я поправлю селекторы

# шаг 2 — сбор (после того как я подтвержу селекторы)
python scripts/owner/collect_interfax.py --collect --years 2019
python scripts/owner/collect_interfax.py --collect          # все годы
```

Сбор возобновляемый: уже проиндексированные сообщения пропускаются, так что
прервать и продолжить можно в любой момент.

**Паузы между запросами в скрипте стоят намеренно и снижать их не надо.** Это
инфраструктура раскрытия, которой пользуются другие люди; вся работа — несколько
тысяч загрузок страниц за вечер, а не гонка. Окно браузера видимое — можно
смотреть, что происходит, и вмешаться.

## Как передать результат

```sh
git clone -b claude/new-project-spec-wx4utz <url репозитория>
# положить файлы в stage-E-1/documents/interfax/
git add stage-E-1/documents/interfax
git commit -m "owner: Interfax message archive 2019-2025"
git push origin claude/new-project-spec-wx4utz
```

Дальше напиши мне — я заберу и продолжу.

## Что я сделаю после

Разберу сообщения в инвентарь, сниму смещение выборки и пропуск 2019 —
июля 2021, проведу слепую классификацию по замороженному чек-листу, поставлю
тег `e1-classified-frozen` и только после него — Фазу B (исходы, плацебо,
вердикт).

---

# Чего от тебя НЕ требуется сейчас

Подписание вердикта E-1 (ТЗ §0.3) — только когда данные будут собраны и
классификация проведена. Сейчас подписывать нечего: половина условия
опровержения не проверена, а торгуемых наблюдений всего пять.

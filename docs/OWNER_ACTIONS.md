# Инструкции владельцу: два действия, разблокирующие вердикт E-1

Оба действия независимы. Первое дешёвое и может закрыть вопрос само по себе;
второе — запасной путь, если первого не хватит.

---

# Действие 1. Открыть домены в окружении (5 минут)

## Зачем

Прокси окружения режет на `403` до установления соединения три группы
хостов. Главный — **НРД**: он раскрывает обязательные предложения, выкупы и
вытеснения **по всей листингованной вселенной** и машиночитаемо. Это ровно та
популяция, которой нет в собранном инвентаре (91% его эмитентов не торгуются
на бирже).

| Хост | Что даёт |
|---|---|
| `nsddata.ru`, `nsd.ru` | корпоративные действия НРД по всем торгуемым бумагам |
| `isin.ru` | справочник ценных бумаг НРД |
| `bo.nalog.ru`, `egrul.nalog.ru` | признак П-5 чек-листа (история приобретателя) без платных агрегаторов |

## Как

1. Открыть **claude.ai/code**.
2. Нажать иконку облака — это селектор окружений. Найти окружение, в котором
   идёт эта сессия, и открыть его на редактирование.
3. Поле **Network access** уже стоит в **Custom** (иначе не работали бы
   `disclosure.ru`, `cbr.ru`, `moex.com`).
4. В поле **Allowed domains** **дописать** строки ниже к тому, что там уже
   есть. **Не заменять список** — иначе отвалятся текущие источники.

```
nsddata.ru
*.nsddata.ru
nsd.ru
*.nsd.ru
isin.ru
*.isin.ru
bo.nalog.ru
egrul.nalog.ru
```

5. Галочка **«Also include default list of common package managers»** должна
   остаться как есть.
6. Сохранить.

## Важные оговорки

- Одна строка — один домен. Ведущая `*.` покрывает все поддомены.
- Изменения подхватывает **новая сессия**; уже запущенная работает со старыми
  правилами. После сохранения напиши мне — если прокси всё ещё отдаёт `403`,
  значит сессию надо перезапустить.
- Если захочешь, чтобы я делал страницы-панели (artifacts) со статусом, в этот
  же список нужна строка `*.frame.claudeusercontent.com` — без неё я не смогу
  читать собственные опубликованные страницы.

## Выполнено 2026-08-25 — результат отрицательный

Владелец список обновил, все шесть хостов отвечают. **НРД надежду не
оправдал:** «Календарь корпоративных действий», ДИСК.СООБЩЕНИЯ и ЦКИ —
платные продукты (лендинги с формой «запросить тестовый доступ»), ЕПКД —
сервис подачи инструкций по логину клиента депозитария. Свободного
машиночитаемого фида корпоративных действий у НРД нет, а уровень расходов 0
закрывает платный. Подробности — `stage-E-1/coverage_report.md`, раздел
«Обновление 2026-08-25 (вечер)».

## Хвост по allowlist — закрыт 2026-08-25

`bo.nalog.ru` редиректил на **`bo.nalog.gov.ru`** — другой домен, которого в
списке не было. Владелец добавил, **`bo.nalog.gov.ru` отвечает HTTP 200** —
ГИР БО доступен для признака П-5. Добавленные строки:

```
bo.nalog.gov.ru
egrul.nalog.gov.ru
*.nalog.gov.ru
```

Сам ЕГРЮЛ при этом закрыт капчей — её я обходить не буду, так что П-5 через
ЕГРЮЛ не автоматизируется; ГИР БО капчи не требует.

Хосты НРД можно из списка убрать — пользы от них нет.

---

# Действие 2. Собрать раскрытие по адресному списку эмитентов

## Что выяснилось на живом сайте (2026-08-25)

Автоматизированный сбор с `e-disclosure.ru` **невозможен, и это проверено, а
не предположено.** Два прогона на машине владельца:

1. Playwright-браузер сам заполняет форму → сайт отвечает капчей: «Ваше
   поведение похоже на поведение автоматизированных систем», головоломка на
   поворот картинки.
2. Тот же браузер, но ведёт человек → `ERR_EMPTY_RESPONSE`, сервер рвёт
   соединение вообще.

Дело не в том, кто нажимает: браузер под управлением Playwright распознаётся
как автоматика независимо от этого. Решать капчу и маскировать браузер здесь
никто не будет — это обход защиты, поставленной владельцем чужого сайта.

Поэтому сбор идёт **в обычном браузере владельца, вручную**, а машинная часть
— только разбор уже сохранённых файлов.

## План стал меньше, а не больше

Слепой перебор всех сообщений четырёх типов за 2019–2025 отменяется. Вместо
него — **74 конкретных эмитента**: `stage-E-1/interfax_worklist.md`.

Это именно те, по которым в материале дыра: торгуемые бумаги, чьё раскрытие
идёт через Интерфакс. Их два источника — события семейства, уже найденные в
AZIPI у торгуемых эмитентов, и 73 делистинга MOEX 2019–2025 (делистинг есть
прямое основание права требовать выкупа по ст. 75, и там же 2019–2020, куда
архив AZIPI не достаёт).

## Что делать

1. Открыть `stage-E-1/interfax_worklist.md` — там таблица с названиями и ИНН.
2. В своём **обычном** браузере на `e-disclosure.ru` найти компанию, открыть
   её ленту раскрытия.
3. Страницы, где встречаются сообщения типов 181/182/184/185, сохранить
   `Cmd+S` → «Веб-страница, только HTML» в
   `stage-E-1/documents/interfax/pages/`.
4. Ничего не нашлось — пропустить. Отрицательный результат тоже результат.

Порядок не важен, останавливаться можно в любой момент, пересохранять
безопасно.

## Разбор

```sh
cd ~/Self_trading_system && source .venv/bin/activate
python scripts/parse_saved_interfax.py \
    --pages stage-E-1/documents/interfax/pages \
    --out stage-E-1/documents/interfax/messages.jsonl
```

Скрипт никуда не ходит — читает только файлы с диска. Дубликаты по `EventId`
отбрасываются, каждое сообщение помечается, относится ли оно к семейству E-1.

## Передать мне

```sh
git add stage-E-1/documents/interfax && git commit -m "owner: Interfax pages" && git push
```

# Чего от тебя НЕ требуется сейчас

Подписание вердикта E-1 (ТЗ §0.3) — только когда данные будут собраны и
классификация проведена. Сейчас подписывать нечего: половина условия
опровержения не проверена, а торгуемых наблюдений всего пять.
